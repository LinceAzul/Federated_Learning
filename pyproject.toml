[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
exclude = ["*.pyc", "*.log", ".git", "venv", "*__pycache__*"]

[project]
name = "FedMSLesSeg"
version = "0.0.1"
dependencies = [
    "torch",
    "numpy",
    "scikit-learn"
]

requires-python = ">=3.13"